# 모든 버전은 핀 고정 권장. 실제 설치 시에는 uv.lock에 박제된 transitive 의존성까지 포함해
# Dockerfile의 `uv sync --frozen`으로 재현성 있게 설치된다.
# pyproject.toml = 원하는 의존성 명세, uv.lock = 실제로 깐 정확한 버전 + 해시.
# 버전 미지정 항목(notebook 등)은 lock에 처음 박제된 시점의 버전이 그대로 유지됨.

[project]
name = "fastapi-scraper"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    # Web framework
    "fastapi==0.133.1",                 # ASGI 웹 프레임워크
    "uvicorn[standard]==0.41.0",        # ASGI 서버 (auto-reload, uvloop 등 포함)
    "python-multipart",                 # multipart/form-data 파싱 (파일 업로드)

    # Validation & settings
    "pydantic==2.12.5",                 # 데이터 검증 / 직렬화
    "pydantic-settings==2.13.1",        # 환경변수 기반 설정 관리

    # Test
    "pytest==9.0.2",                    # 테스트 프레임워크
    "pytest-asyncio==1.3.0",            # pytest 비동기 지원

    # Notebook (실험/탐색용)
    "notebook==7.5.5",                  # Jupyter Notebook 서버 (notebooks/ 디렉터리 사용)
    "ipywidgets==8.1.8",                # Jupyter 위젯 지원 (tqdm progress bar, interactive UI 등)

    # Async task queue
    "redis==7.4.0",                     # Redis 클라이언트 (arq 작업 큐 broker/result)

    # Auth
    "python-jose[cryptography]",        # JWT 인코딩/디코딩

    # HTTP client
    "httpx",                            # 비동기 HTTP 클라이언트 (외부 API 호출용)

    # Database
    "SQLAlchemy==2.0.47",               # MySQL ORM
    "pymysql==1.1.2",                   # MySQL 동기 드라이버
    "aiomysql==0.3.2",                  # MySQL 비동기 드라이버
    "alembic==1.18.4",                  # DB 마이그레이션 도구

    "groq",

    # Dev utils
    "python-dotenv",                    # .env 파일 로더

    # Browser automation
    "playwright==1.58.0",               # 브라우저 자동화 (Tistory 로그인/글쓰기 자동화)
]


# .docker/Dockerfile 에서 수행
# uv run playwright install chromium, Playwright 브라우저 엔진 추가 설치
# uv run playwright install-deps, chromium 실행에 필요한 모든 리눅스 패키지 설치